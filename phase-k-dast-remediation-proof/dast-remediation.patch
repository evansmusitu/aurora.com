diff --git a/apps/musitu-mobile/musitu_mobile/server.py b/apps/musitu-mobile/musitu_mobile/server.py
index cddbd7f..fdfec96 100644
--- a/apps/musitu-mobile/musitu_mobile/server.py
+++ b/apps/musitu-mobile/musitu_mobile/server.py
@@ -31,16 +31,27 @@ def _build_service() -> ZeroSimLabService:
 
 SERVICE = _build_service()
 APP_ROOT = Path(__file__).resolve().parents[1]
+MAX_JSON_BODY_BYTES = 64 * 1024
 
 
 class Handler(BaseHTTPRequestHandler):
     service = SERVICE
+    server_version = "MUSITU"
+    sys_version = ""
+
+    def version_string(self) -> str:
+        return self.server_version
+
+    def _send_security_headers(self) -> None:
+        self.send_header("X-Content-Type-Options", "nosniff")
+        self.send_header("Cross-Origin-Resource-Policy", "same-origin")
 
     def _send_json(self, status: int, payload: dict) -> None:
         body = json.dumps(payload, sort_keys=True).encode()
         self.send_response(status)
         self.send_header("Content-Type", "application/json")
         self.send_header("Content-Length", str(len(body)))
+        self._send_security_headers()
         self.end_headers()
         self.wfile.write(body)
 
@@ -48,9 +59,36 @@ class Handler(BaseHTTPRequestHandler):
         self.send_response(status)
         self.send_header("Content-Type", content_type)
         self.send_header("Content-Length", str(len(body)))
+        self._send_security_headers()
         self.end_headers()
         self.wfile.write(body)
 
+    def _read_json_object(self) -> dict | None:
+        raw_length = self.headers.get("Content-Length", "0")
+        try:
+            length = int(raw_length)
+        except (TypeError, ValueError):
+            self.close_connection = True
+            self._send_json(400, {"error": "invalid_content_length"})
+            return None
+        if length < 0:
+            self.close_connection = True
+            self._send_json(400, {"error": "invalid_content_length"})
+            return None
+        if length > MAX_JSON_BODY_BYTES:
+            self.close_connection = True
+            self._send_json(413, {"error": "request_too_large"})
+            return None
+        try:
+            payload = json.loads(self.rfile.read(length) or b"{}")
+        except (json.JSONDecodeError, UnicodeDecodeError):
+            self._send_json(400, {"error": "invalid_json"})
+            return None
+        if not isinstance(payload, dict):
+            self._send_json(400, {"error": "invalid_json"})
+            return None
+        return payload
+
     def do_GET(self):  # noqa: N802
         if self.path == "/":
             self._send_bytes(200, (APP_ROOT / "index.html").read_bytes(), "text/html; charset=utf-8")
@@ -75,11 +113,8 @@ class Handler(BaseHTTPRequestHandler):
 
     def do_POST(self):  # noqa: N802
         if self.path == "/v1/onboarding":
-            length = int(self.headers.get("Content-Length", "0"))
-            try:
-                payload = json.loads(self.rfile.read(length) or b"{}")
-            except json.JSONDecodeError:
-                self._send_json(400, {"error": "invalid_json"})
+            payload = self._read_json_object()
+            if payload is None:
                 return
             device_id = payload.get("device_id")
             if not isinstance(device_id, str) or not device_id.strip():
@@ -88,11 +123,8 @@ class Handler(BaseHTTPRequestHandler):
             self._send_json(200, {"device_id": device_id, "profiles": self.service.list_profiles(device_id)})
             return
         if self.path == "/v1/zerosim/activate":
-            length = int(self.headers.get("Content-Length", "0"))
-            try:
-                payload = json.loads(self.rfile.read(length) or b"{}")
-            except json.JSONDecodeError:
-                self._send_json(400, {"error": "invalid_json"})
+            payload = self._read_json_object()
+            if payload is None:
                 return
             device_id = payload.get("device_id")
             activation_code = payload.get("activation_code")
diff --git a/edge/musitu_edge/management.py b/edge/musitu_edge/management.py
index 7bf2e1a..8e956f7 100644
--- a/edge/musitu_edge/management.py
+++ b/edge/musitu_edge/management.py
@@ -26,6 +26,12 @@ class EdgeManagementServer:
         }
 
         class Handler(BaseHTTPRequestHandler):
+            server_version = "MUSITU"
+            sys_version = ""
+
+            def version_string(self) -> str:
+                return self.server_version
+
             def do_GET(self) -> None:  # noqa: N802
                 if self.path == "/healthz":
                     self._write_json(200, {"status": "ok", "service": "MUSITU Edge"})
@@ -40,6 +46,8 @@ class EdgeManagementServer:
                 self.send_response(status)
                 self.send_header("content-type", "application/json")
                 self.send_header("content-length", str(len(body)))
+                self.send_header("X-Content-Type-Options", "nosniff")
+                self.send_header("Cross-Origin-Resource-Policy", "same-origin")
                 self.end_headers()
                 self.wfile.write(body)
 
diff --git a/services/identity/musitu_identity/server.py b/services/identity/musitu_identity/server.py
index df73825..d0eadcc 100644
--- a/services/identity/musitu_identity/server.py
+++ b/services/identity/musitu_identity/server.py
@@ -4,11 +4,19 @@ from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer
 
 
 class Handler(BaseHTTPRequestHandler):
+    server_version = "MUSITU"
+    sys_version = ""
+
+    def version_string(self) -> str:
+        return self.server_version
+
     def _send_json(self, status: int, payload: dict) -> None:
         body = json.dumps(payload, sort_keys=True).encode()
         self.send_response(status)
         self.send_header("Content-Type", "application/json")
         self.send_header("Content-Length", str(len(body)))
+        self.send_header("X-Content-Type-Options", "nosniff")
+        self.send_header("Cross-Origin-Resource-Policy", "same-origin")
         self.end_headers()
         self.wfile.write(body)
 
diff --git a/services/network-api/musitu_control/server.py b/services/network-api/musitu_control/server.py
index 34cf8ae..acce5b6 100644
--- a/services/network-api/musitu_control/server.py
+++ b/services/network-api/musitu_control/server.py
@@ -16,6 +16,7 @@ ROOT = Path(__file__).resolve().parents[3]
 DASHBOARD = ROOT / "apps" / "musitu-control-center" / "index.html"
 CAMARA_DEVICE_MAP_PATH = os.getenv("MUSITU_CAMARA_DEVICE_MAP")
 CAMARA_DEVICE_MAP = CamaraDeviceMap(CAMARA_DEVICE_MAP_PATH) if CAMARA_DEVICE_MAP_PATH else None
+MAX_JSON_BODY_BYTES = 64 * 1024
 
 
 def _probe_health(url: str) -> dict:
@@ -79,6 +80,15 @@ def build_provider() -> StateProvider:
 
 class Handler(BaseHTTPRequestHandler):
     provider: StateProvider = build_provider()
+    server_version = "MUSITU"
+    sys_version = ""
+
+    def version_string(self) -> str:
+        return self.server_version
+
+    def _send_security_headers(self) -> None:
+        self.send_header("X-Content-Type-Options", "nosniff")
+        self.send_header("Cross-Origin-Resource-Policy", "same-origin")
 
     def _send_json(self, status: int, payload: dict, headers: dict[str, str] | None = None) -> None:
         body = json.dumps(payload, sort_keys=True).encode()
@@ -87,6 +97,7 @@ class Handler(BaseHTTPRequestHandler):
         self.send_header("Content-Length", str(len(body)))
         for name, value in (headers or {}).items():
             self.send_header(name, value)
+        self._send_security_headers()
         self.end_headers()
         self.wfile.write(body)
 
@@ -95,9 +106,48 @@ class Handler(BaseHTTPRequestHandler):
         self.send_response(status)
         self.send_header("Content-Type", "text/html; charset=utf-8")
         self.send_header("Content-Length", str(len(encoded)))
+        self._send_security_headers()
         self.end_headers()
         self.wfile.write(encoded)
 
+    def _read_camara_json_object(self, response_headers: dict[str, str] | None) -> dict | None:
+        invalid_argument = {
+            "status": 400,
+            "code": "INVALID_ARGUMENT",
+            "message": "Client specified an invalid argument, request body or query param.",
+        }
+        raw_length = self.headers.get("Content-Length", "0")
+        try:
+            length = int(raw_length)
+        except (TypeError, ValueError):
+            self.close_connection = True
+            self._send_json(400, invalid_argument, response_headers)
+            return None
+        if length < 0:
+            self.close_connection = True
+            self._send_json(400, invalid_argument, response_headers)
+            return None
+        if length > MAX_JSON_BODY_BYTES:
+            self.close_connection = True
+            self._send_json(
+                413,
+                {
+                    "status": 413,
+                    "code": "REQUEST_TOO_LARGE",
+                    "message": "Request body exceeds the MUSITU lab limit.",
+                },
+                response_headers,
+            )
+            return None
+        try:
+            payload = json.loads(self.rfile.read(length) or b"{}")
+            if not isinstance(payload, dict):
+                raise ValueError("CAMARA request body must be a JSON object")
+        except (json.JSONDecodeError, UnicodeDecodeError, ValueError):
+            self._send_json(400, invalid_argument, response_headers)
+            return None
+        return payload
+
     def do_GET(self) -> None:  # noqa: N802
         if self.path == "/":
             self._send_html(200, DASHBOARD.read_text())
@@ -122,21 +172,8 @@ class Handler(BaseHTTPRequestHandler):
         if self.path == "/device-reachability-status/v1/retrieve":
             correlator = self.headers.get("x-correlator")
             response_headers = {"x-correlator": correlator} if correlator else None
-            length = int(self.headers.get("Content-Length", "0"))
-            try:
-                payload = json.loads(self.rfile.read(length) or b"{}")
-                if not isinstance(payload, dict):
-                    raise ValueError("CAMARA request body must be a JSON object")
-            except (json.JSONDecodeError, UnicodeDecodeError, ValueError):
-                self._send_json(
-                    400,
-                    {
-                        "status": 400,
-                        "code": "INVALID_ARGUMENT",
-                        "message": "Client specified an invalid argument, request body or query param.",
-                    },
-                    response_headers,
-                )
+            payload = self._read_camara_json_object(response_headers)
+            if payload is None:
                 return
             if CAMARA_DEVICE_MAP is None:
                 self._send_json(
